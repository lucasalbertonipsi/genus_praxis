{
  "name": "genus-praxis",
  "version": "1.0.0",
  "description": "Genus Praxis — Plataforma de Simulação Clínica (white label All_OS, estética laranja)",
  "scripts": {
    "server": "node server/index.js",
    "client": "cd client && npx vite --host",
    "dev": "npm run install:client && concurrently \"npm run server\" \"npm run client\"",
    "install:client": "cd client && npm install --include=dev",
    "//build": "--include=dev é obrigatório: o Railway define NODE_ENV=production, e o vite é devDependency do client — sem a flag o build quebra com ERR_MODULE_NOT_FOUND.",
    "build": "npm run install:client && cd client && npx vite build",
    "start": "node server/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "3.0.3",
    "cors": "2.8.6",
    "dotenv": "17.4.1",
    "express": "4.22.2",
    "express-rate-limit": "7.5.1",
    "jsonwebtoken": "9.0.3",
    "openai": "6.38.0"
  },
  "devDependencies": {
    "concurrently": "8.2.2",
    "supertest": "^7.2.2",
    "vitest": "^2.1.9"
  }
}
